# GitHub organizations with private repo access
# When source.github.owner matches an org here, GitHub provider is used
orgs:
  tini-works:
    githubId: W6uKDj7mTCtzLWplO_Z5m  # GitHub OAuth app with tini-works access
    sshKeyId: T0gw6iBOVzWT7-lq_Gqvc   # Fallback SSH key (deprecated)
    description: "tini-works org - uses GitHub provider for private repos"
