# GitHub organizations with SSH key access for private repos
# When source.github.owner matches an org here, SSH cloning is used
orgs:
  tini-works:
    sshKeyId: T0gw6iBOVzWT7-lq_Gqvc
    description: "tini-works org deploy key (proto-medalpha)"
