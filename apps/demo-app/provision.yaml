apiVersion: provisioner.quickable.co/v1
kind: Application
metadata:
  name: demo-app
  description: "Demo application for validation"
  maintainer: "@lagz0ne"
spec:
  source:
    type: github
    github:
      owner: "quickable"
      repo: "test-app"
      branch: "main"
  build:
    type: dockerfile
  resources:
    size: S
  ports:
    - containerPort: 3000
  healthCheck:
    path: "/health"
    port: 3000
